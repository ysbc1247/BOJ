#include<iostream>
#include<algorithm>
#include<string>
#include<vector>
#include<cmath>
#include<iomanip>
#include<map>
#include<set>
#include<queue>
#include<functional>
#include<regex>
#include<stack>
#include<deque>
#include<list>
#define endl "\n"
#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
typedef long long ll;
using namespace std;




int main() {
	fastio;
	
	int k;
	cin >> k;
	cout << 2000<<endl;
	for (int i = 0; i < 1000; i++) {
		cout << 1 << ' ';
	}
	for (int i = 0; i < 1000; i++) {
		cout << 1000 << ' ';
	}

}
